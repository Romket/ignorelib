# get yoinked leah-nyx

name: pre-commit

on:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get upgrade

      - name: Download abseil
        run: |
          sudo apt-get install abseil-cpp

      - name: Install Clang-Format 22
        run: |
          wget https://apt.llvm.org/llvm.sh
          chmod +x llvm.sh
          sudo ./llvm.sh 22
          sudo apt-get install -y clang-format-22
          # Create a symlink so 'clang-format' defaults to v22
          sudo ln -s /usr/bin/clang-format-22 /usr/local/bin/clang-format

      - name: Set up Python  # Recommended way to install pre-commit (supposedly)
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install pre-commit
        run: pip install pre-commit

      - name: Setup pre-commit
        run: pre-commit install

      - name: Run pre-commit
        run: pre-commit run --all-files
